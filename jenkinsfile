  pipeline {
 
    options {
        skipDefaultCheckout true
    } 
    stages {
        stage("SCM Checkout") {
            Checkout SCM
        }
    

		stage ('Compile-Package'){
		sh 'mvn clean compile test-compile'
    }
   }
}